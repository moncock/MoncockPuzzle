{
  "name": "moncock-puzzle",
  "version": "1.0.0",
  "description": "Match and Mint Puzzle Game — deploy on Netlify with automatic asset randomization",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build:list": "node -e \"const fs=require('fs');const p='docs/asset/images';const out='docs/asset/list.json';const files=fs.readdirSync(p).filter(f=>/\\.(png|jpg|jpeg|gif|svg)$/i.test(f)).sort();fs.writeFileSync(out, JSON.stringify(files,null,2)+'\\n');console.log('Wrote',out,'with',files.length,'items');\""
  },
  "dependencies": {
    "form-data": "^4.0.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {},
  "license": "MIT"
}
